

let firstName;
let lastName;

// console.log(`${ firstName || 'No first name' } ${lastName || 'No last name' }`);

const isActive = true;

const message = ( isActive ) ? 'Activo' : 'Inactivo';

// if ( isActive ) {
//     message = 'Activo';
// } else {
//     message = 'Inactivo'
// }

console.log( message );